import type { Metadata } from 'next';

export function generateMetadata(): Metadata {
  return { title: 'Страницы | Умная зарядка — Саратов' };
}

export default function Page() {
  return (
    <>
      <div>Страницы — Coming Soon</div>
    </>
  );
}
