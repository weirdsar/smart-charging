import type { Metadata } from 'next';

export function generateMetadata(): Metadata {
  return { title: 'Настройки | Умная зарядка — Саратов' };
}

export default function Page() {
  return (
    <>
      <div>Настройки — Coming Soon</div>
    </>
  );
}
