import type { Metadata } from 'next';

export function generateMetadata(): Metadata {
  return { title: 'SEO-страницы фильтров | Умная зарядка — Саратов' };
}

export default function Page() {
  return (
    <>
      <div>SEO-страницы фильтров — Coming Soon</div>
    </>
  );
}
